//- Fragment layout consumed via `extends`. Editing this file must invalidate
//- every template that extends it — exercises `extends` dependency tracking.
.l-demo-shell
  .l-demo-shell__bar
    block toolbar
  .l-demo-shell__body
    block content
